# 部署步骤详细说明

## 🎯 在哪儿输入命令？

**在 Cursor 的 Terminal（终端）中输入！**

### 打开 Terminal 的方法

1. **方法 1（推荐）**：
   - 在 Cursor 中按快捷键：`Ctrl + `` (反引号，在数字1左边)
   - 或者：`View` → `Terminal`

2. **方法 2**：
   - 点击 Cursor 底部的 "Terminal" 标签

3. **方法 3**：
   - 菜单栏：`Terminal` → `New Terminal`

## 📋 完整部署步骤

### 第一步：检查当前状态

在 Terminal 中输入：

```bash
# 确认当前目录
pwd

# 应该显示：/Users/tingyuzheng/Downloads/us_china_picker
```

### 第二步：初始化 Git（如果还没有）

```bash
# 检查是否已经是 Git 仓库
git status

# 如果不是，初始化
git init
```

### 第三步：检查要提交的文件

```bash
# 查看哪些文件会被提交
git status

# 应该看到所有 .py, .yaml, .md 文件
# 不应该看到 .env, google_credentials.json 等敏感文件
```

### 第四步：添加到 Git

```bash
# 添加所有文件（.gitignore 会自动排除敏感文件）
git add .

# 再次检查
git status
```

### 第五步：创建首次提交

```bash
# 提交代码
git commit -m "Initial commit: US-China news scraper"
```

### 第六步：在 GitHub 创建仓库

1. **打开浏览器**，访问 https://github.com
2. **登录**你的 GitHub 账号
3. **点击右上角的 "+"** → "New repository"
4. **填写信息**：
   - Repository name: `us-china-picker` (或你喜欢的名字)
   - Description: "US-China news scraper and categorizer"
   - **选择 Public** 或 Private（根据你的需要）
   - **不要**勾选 "Initialize this repository with a README"（因为我们已经有了）
5. **点击 "Create repository"**

### 第七步：连接本地仓库到 GitHub

在 Terminal 中输入（**替换 your-username**）：

```bash
# 将 your-username 替换为你的 GitHub 用户名
git remote add origin https://github.com/your-username/us-china-picker.git

# 或者如果你使用 SSH（需要配置 SSH key）
# git remote add origin git@github.com:your-username/us-china-picker.git
```

### 第八步：推送代码到 GitHub

```bash
# 设置主分支
git branch -M main

# 推送代码
git push -u origin main
```

**如果提示输入账号密码**：
- 使用 GitHub Personal Access Token（不是密码）
- 或者配置 SSH key（更安全）

### 第九步：部署到 Streamlit Cloud

1. **打开浏览器**，访问 https://streamlit.io/cloud
2. **用 GitHub 账号登录**
3. **点击 "New app"**
4. **填写信息**：
   - Repository: 选择刚才创建的 `us-china-picker`
   - Branch: `main`
   - **Main file path**: `app_with_sheets_db.py`（推荐）或 `app.py`
5. **点击 "Deploy"**
6. **等待 2-3 分钟**

### 第十步：配置 Secrets（如果需要 Google Sheets 或 API）

1. **在 Streamlit Cloud**，打开你的应用
2. **点击 Settings** → **Secrets**
3. **添加配置**（如果需要）：

```toml
# Google Sheets（如果需要）
GOOGLE_SHEETS_ID = "your-spreadsheet-id"

# API 分类（如果需要）
[api]
classifier_enabled = true
provider = "openai"
openai_api_key = "sk-your-key"
```

## ⚠️ 常见问题

### 问题 1: git push 需要认证

**解决方法**：
- 使用 GitHub Personal Access Token
- 访问：https://github.com/settings/tokens
- 创建新 token（选择 `repo` 权限）
- 使用 token 作为密码

### 问题 2: 不知道 GitHub 用户名

**解决方法**：
- 登录 GitHub
- 右上角头像 → Settings
- 左侧菜单显示你的用户名

### 问题 3: 想用 SSH 而不是 HTTPS

**配置 SSH key**（可选）：
```bash
# 生成 SSH key
ssh-keygen -t ed25519 -C "your-email@example.com"

# 复制公钥
cat ~/.ssh/id_ed25519.pub

# 添加到 GitHub: Settings → SSH and GPG keys → New SSH key
```

### 问题 4: Terminal 显示 "command not found"

**解决方法**：
- 确保在正确的目录：`cd /Users/tingyuzheng/Downloads/us_china_picker`
- 检查 Git 是否安装：`git --version`

## 📝 快速命令列表

**全部复制粘贴到 Terminal（一行一行执行）**：

```bash
# 1. 确认目录
cd /Users/tingyuzheng/Downloads/us_china_picker

# 2. 初始化 Git（如果还没有）
git init

# 3. 检查文件
git status

# 4. 添加文件
git add .

# 5. 提交
git commit -m "Initial commit: US-China news scraper"

# 6. 连接 GitHub（替换 your-username）
git remote add origin https://github.com/your-username/us-china-picker.git

# 7. 设置分支并推送
git branch -M main
git push -u origin main
```

## 🎉 完成！

推送成功后，去 Streamlit Cloud 部署即可！

**记住**：
- Terminal 在 Cursor 底部
- 快捷键：`Ctrl + `` (反引号)
- 每行命令执行后按 Enter

