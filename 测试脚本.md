# 测试脚本

## 🧪 测试步骤

### 1. 测试数据收集（不需要凭证）

```bash
python3 -c "
from collector import collect
from datetime import date

# 测试收集今天的文章
df = collect(
    'config_en.yaml',
    date.today().isoformat(),
    date.today().isoformat(),
    us_china_only=False,
    limit_sources=['nytimes.com', 'scmp.com', 'reuters.com']
)

print(f'✅ 收集了 {len(df)} 篇文章')
print(df[['Date', 'Outlet', 'Headline']].head())
"
```

### 2. 测试完整上传（需要凭证文件）

**前提条件**：
- 已下载 `google_credentials.json` 文件
- 已分享 Google Sheets 给 Service Account

**设置环境变量**：
```bash
export GOOGLE_SHEETS_ID="1Cltg8pq-jhtgR6_lysW-gNIe9JSKOh2vCT4Pxo7pcpA"
export GOOGLE_CREDENTIALS_PATH="/Users/tingyuzheng/Downloads/us_china_picker/google_credentials.json"
```

**运行测试**：
```bash
python3 daily_collector_to_sheets.py
```

### 3. 测试网站功能（需要 Streamlit Cloud Secrets）

1. 打开你的 Streamlit Cloud 网站
2. 勾选 "从 Google Sheets 读取历史数据"
3. 输入 Spreadsheet ID
4. 点击 "Generate & Export"
5. 检查是否能成功读取数据

## ✅ 检查清单

- [ ] 数据收集功能正常
- [ ] 凭证文件已下载
- [ ] Google Sheets 已分享
- [ ] 定时任务脚本可以上传
- [ ] 网站可以读取 Google Sheets

