# 🔍 测试模式问题排查指南

## 问题：配置了测试模式但没有看到密码输入界面

### 可能的原因和解决方案

#### 1. **Secrets 配置格式问题**

在 Streamlit Cloud Secrets 中，确保配置格式正确：

**✅ 正确格式：**
```toml
[test_mode]
enabled = true
password = "test2025"
deadline = "2025-11-14"
```

**或者（也支持）：**
```toml
[test_mode]
enabled = "true"
password = "test2025"
deadline = "2025-11-14"
```

**❌ 错误格式：**
```toml
[test_mode]
enabled = "True"  # 大写 T，错误
enabled = True    # Python 布尔值，在 TOML 中应该是小写 true
password = test2025  # 没有引号，错误
```

#### 2. **检查 Secrets 是否已保存**

1. 在 Streamlit Cloud 中，进入你的应用
2. 点击 "Settings" → "Secrets"
3. 确认 `[test_mode]` 配置已保存
4. 点击 "Save" 确保配置已保存

#### 3. **应用是否已重新加载**

- 更新 Secrets 后，Streamlit Cloud 会自动重新加载应用
- 如果没看到变化，尝试：
  - 刷新浏览器页面
  - 清除浏览器缓存
  - 等待 10-30 秒让应用重新加载

#### 4. **检查代码是否已更新**

- 确保最新的代码已推送到 GitHub
- Streamlit Cloud 会自动拉取最新代码
- 如果代码刚修复，可能需要等待几分钟

#### 5. **调试步骤**

如果仍然没有看到测试模式，可以：

1. **检查应用日志**：
   - 在 Streamlit Cloud 中，查看应用日志
   - 查找是否有错误信息

2. **验证配置**：
   - 在 Streamlit Cloud Secrets 中，确认配置格式正确
   - 确保 `enabled = true`（或 `enabled = "true"`）
   - 确保 `password` 和 `deadline` 都已设置

3. **测试配置**：
   - 尝试禁用测试模式：`enabled = false`
   - 保存后，应该可以直接访问（不需要密码）
   - 然后重新启用：`enabled = true`
   - 保存后，应该会看到密码输入界面

#### 6. **常见错误**

| 错误 | 原因 | 解决方案 |
|------|------|----------|
| 没有看到密码输入界面 | `enabled` 不是 `true` | 检查 `enabled = true` |
| 密码输入后无法访问 | 密码不匹配 | 检查 `password = "test2025"` |
| 显示"测试阶段已结束" | 截止日期已过 | 更新 `deadline` 为未来日期 |
| 应用崩溃 | Secrets 格式错误 | 检查 TOML 格式是否正确 |

#### 7. **正确的完整配置示例**

```toml
GOOGLE_SHEETS_ID = "your-sheets-id"

[google_sheets]
credentials = '''
{
  "type": "service_account",
  ...
}
'''

[api]
classifier_enabled = false
provider = "openai"
openai_api_key = ""

[test_mode]
enabled = true
password = "test2025"
deadline = "2025-11-14"
```

---

## 📞 如果问题仍然存在

1. 检查 Streamlit Cloud 应用日志
2. 确认代码已推送到 GitHub
3. 确认 Secrets 配置格式正确
4. 尝试禁用并重新启用测试模式

