# 模型切换、反馈机制和多类别分类 FAQ

## 1. 反馈到第20个时会消失吗？

### ❌ 不会消失！

**反馈文件会一直保存**：
- 所有反馈都保存在 `classification_feedback.json` 文件中
- 这个文件会一直存在，不会自动删除
- 即使反馈超过 20 个，**所有反馈都会保留在文件中**

**只是不会在 prompt 中使用**：
- API 调用时，只取最近 20 个 "incorrect" 反馈
- 旧的反馈仍然在文件中，只是不会影响 AI 的分类
- 如果将来增加到 30 个或 50 个，旧的反馈会重新生效

**示例**：
```
反馈文件中有 50 个反馈
→ API 调用时只使用最后 20 个
→ 前面的 30 个不会影响分类
→ 但文件中的 50 个反馈都还在
→ 如果改成使用 50 个，所有反馈都会生效
```

### 如何查看所有反馈？

```bash
# 查看所有反馈
cat classification_feedback.json

# 查看反馈总数
cat classification_feedback.json | jq 'length'

# 查看 "incorrect" 反馈数量
cat classification_feedback.json | jq '[.[] | select(.status == "incorrect")] | length'
```

## 2. 如何切换不同的模型？

### 方法 1：修改 secrets.toml（推荐）

编辑 `.streamlit/secrets.toml`：

```toml
[api]
classifier_enabled = true
provider = "openai"
openai_api_key = "sk-你的-API-key"
openai_model = "gpt-4o-mini"  # 修改这里
```

**可用的模型**：
- `gpt-4o-mini`（当前，便宜，速度快）
- `gpt-4o`（更准确，但更贵）
- `gpt-4-turbo`（旧版本）
- `gpt-3.5-turbo`（最便宜，但准确度较低）

### 方法 2：环境变量

```bash
export OPENAI_MODEL="gpt-4o"
```

### 切换后之前的训练会消失吗？

**❌ 不会消失！**

**反馈文件是独立的**：
- `classification_feedback.json` 保存在本地文件系统中
- 与模型无关，切换模型后仍然存在
- 所有反馈都会保留

**但需要注意**：
- 不同模型对 prompt 的响应可能不同
- GPT-4o 可能比 GPT-4o-mini 更准确
- 但反馈文件本身不会丢失

**建议**：
- 切换模型后，先用少量文章测试效果
- 如果新模型效果更好，可以继续使用
- 如果效果不好，可以切换回原模型

## 3. Fine-tuning 的原理是什么？

### 基本概念

**Fine-tuning（微调）** 是在预训练模型的基础上，使用你的特定数据进一步训练模型，使其更适应你的任务。

### 工作流程

```
1. 基础模型（GPT-4o-mini）
   ↓
2. 准备训练数据（你的反馈 + 正确分类）
   ↓
3. 上传到 OpenAI
   ↓
4. 训练（几小时到几天）
   ↓
5. 得到定制模型（your-model-id）
   ↓
6. 使用定制模型（更准确，prompt 更短）
```

### 与当前 Few-shot Learning 的区别

**当前方式（Few-shot Learning）**：
- 每次 API 调用时，在 prompt 中包含 20 个反馈示例
- 模型根据这些示例进行推理
- **优点**：简单，不需要训练
- **缺点**：每次都要发送长 prompt，成本较高

**Fine-tuning**：
- 用你的反馈数据训练模型
- 模型"记住"了这些模式
- **优点**：prompt 更短，更准确，成本更低（长期）
- **缺点**：需要训练成本，修改规则需要重新训练

### Fine-tuning 的成本

**训练成本**（一次性）：
- GPT-4o-mini: $8-80（取决于数据量）
- GPT-4o: 更贵

**使用成本**（每次调用）：
- 与基础模型相同或略高

**何时值得 Fine-tuning？**
- 反馈数据超过 500 个
- 规则相对稳定
- 使用量大（每天 > 500 篇文章）

## 4. 多类别分类问题（Geopolitics vs Taiwan）

### 问题描述

有些文章确实可以属于多个类别，例如：
- "日本就台湾问题发出不当言论，中国警告"
- 可以是 `Geopolitics`（地缘政治）
- 也可以是 `Taiwan`（台湾问题）

### 当前策略

**当前 prompt 中的规则**：
```
1. Choose the MOST SPECIFIC category that best fits the article
2. If an article could fit multiple categories, choose the PRIMARY focus
```

**这意味着**：
- 如果文章主要讲台湾问题，选择 `Taiwan`
- 如果文章主要讲地缘政治关系，选择 `Geopolitics`
- 选择"最相关"的类别

### 改进方案

#### 方案 1：改进 Prompt（推荐，简单）

在 prompt 中添加更明确的规则：

```python
**Multi-category articles (choose the MOST SPECIFIC category):**
- If an article is about Taiwan AND involves other countries (Japan, US, etc.), choose "Taiwan" (more specific)
- If an article is about general geopolitical relations involving China, choose "Geopolitics" (broader)
- Example: "Japan warns China over Taiwan" → Taiwan (specific issue)
- Example: "China-Japan relations strained by multiple issues" → Geopolitics (broader relations)
```

#### 方案 2：多标签分类（复杂，需要改数据结构）

允许一篇文章有多个类别：
- 需要修改数据结构（Google Sheets 需要支持多值）
- 需要修改 prompt（返回多个类别）
- 需要修改 UI（显示多个类别）

**不推荐**：因为会增加复杂度，且大多数情况下一个主要类别就够了。

#### 方案 3：二级分类（折中）

保留主要类别，但添加"相关类别"字段：
- 主要类别：`Taiwan`
- 相关类别：`Geopolitics`

**不推荐**：增加复杂度，收益有限。

### 推荐做法

**改进 prompt**，添加更明确的规则：

```python
**Multi-category handling:**
- If an article could fit multiple categories, choose the MOST SPECIFIC one
- For Taiwan-related articles:
  - If primarily about Taiwan issue → "Taiwan"
  - If about broader regional relations → "Geopolitics"
- For trade-related articles:
  - If primarily about tariffs/sanctions → "Trade & Commerce"
  - If about broader economic relations → "Business & Investment"
- Always choose the category that BEST describes the PRIMARY focus
```

## 📝 总结

1. **反馈不会消失**：文件会一直保存，只是 API 调用时只取最近 20 个
2. **切换模型**：修改 `secrets.toml` 中的 `openai_model`，反馈文件不会丢失
3. **Fine-tuning**：用你的数据训练定制模型，更准确但需要训练成本
4. **多类别问题**：改进 prompt，明确选择"最相关"的类别

