# 部署指南 - 给不懂代码的同事使用

## 🎯 目标

创建一个**稳定的网站**，让同事可以：
1. 打开网站
2. 选择日期范围
3. 点击按钮
4. 下载 Excel 或自动上传到 Google Sheets

## 🚀 推荐方案：Streamlit Cloud（免费，最简单）

### 优点
- ✅ **完全免费**
- ✅ **自动部署**，代码更新自动生效
- ✅ **稳定可靠**，由 Streamlit 官方维护
- ✅ **自定义域名**支持（可选）
- ✅ **无需服务器**，无需维护

### 部署步骤

#### 1. 准备 GitHub 仓库

```bash
# 在项目目录下
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/your-username/us-china-picker.git
git push -u origin main
```

#### 2. 部署到 Streamlit Cloud

1. **访问 Streamlit Cloud**
   - 打开 https://streamlit.io/cloud
   - 使用 GitHub 账号登录

2. **创建新应用**
   - 点击 "New app"
   - 选择你的 GitHub 仓库
   - 设置：
     - **Main file path**: `app.py` 或 `app_with_sheets.py`
     - **Python version**: 3.11
   - 点击 "Deploy"

3. **等待部署**
   - 通常 2-3 分钟完成
   - 部署完成后会得到一个 URL，例如：`https://us-china-picker.streamlit.app`

4. **设置自定义域名（可选）**
   - 在 Streamlit Cloud 设置中添加自定义域名
   - 需要域名 DNS 配置

#### 3. 配置环境变量（如果需要）

如果需要使用数据库或 Google Sheets：
- 在 Streamlit Cloud 的 Settings 中添加环境变量
- 或者使用 Streamlit Secrets（更安全）

### 使用方式

**给同事的说明**：

1. 打开网站：`https://your-app.streamlit.app`
2. 选择日期范围
3. 点击 "Generate & Export"
4. 下载 Excel 文件

**就这么简单！** 🎉

## 📊 方案 2：添加 Google Sheets 集成（可选）

### 设置 Google Sheets API

#### 1. 创建 Google Service Account

1. 访问 https://console.cloud.google.com
2. 创建新项目或选择现有项目
3. 启用 Google Sheets API 和 Google Drive API
4. 创建 Service Account：
   - APIs & Services → Credentials → Create Credentials → Service Account
   - 下载 JSON 凭证文件
   - 命名为 `google_credentials.json`

#### 2. 共享 Google Sheets

1. 创建或打开 Google Sheets
2. 点击 "Share"（分享）
3. 添加 Service Account 的邮箱（从 JSON 文件中获取）
4. 给予 "Editor" 权限

#### 3. 获取 Spreadsheet ID

从 Google Sheets URL 中获取：
```
https://docs.google.com/spreadsheets/d/SPREADSHEET_ID/edit
                                    ↑ 这部分就是 ID
```

#### 4. 部署配置

- 将 `google_credentials.json` 添加到项目中（**不要提交到 GitHub！**）
- 或者使用 Streamlit Secrets 存储凭证

### 使用 Google Sheets 功能

在网站中：
1. 勾选 "Export to Google Sheets"
2. 输入 Google Sheets ID
3. 输入 Sheet 名称（可选，会自动使用日期范围）
4. 点击 "Generate & Export"
5. 数据会自动上传到 Google Sheets

## 🔄 方案 3：Railway（如果需要更多控制）

### 优点
- 更多配置选项
- 支持自定义域名
- $5/月免费额度

### 部署步骤

1. **访问 Railway**
   - https://railway.app
   - 使用 GitHub 登录

2. **创建新项目**
   - New Project → Deploy from GitHub repo
   - 选择你的仓库

3. **配置**
   - 设置启动命令：`streamlit run app.py --server.port $PORT`
   - 添加环境变量（如果需要）

4. **部署**
   - Railway 会自动部署
   - 获得一个 URL

## 📝 交接清单

### 给同事的文档

创建 `使用说明.md`：

```markdown
# 使用说明

## 访问网站
打开：https://your-app.streamlit.app

## 使用步骤
1. 选择开始日期和结束日期
2. 选择新闻来源（默认全选）
3. 点击 "Generate & Export"
4. 等待加载完成
5. 下载 Excel 文件

## Google Sheets 导出（可选）
1. 勾选 "Export to Google Sheets"
2. 输入 Google Sheets ID
3. 输入 Sheet 名称（可选）
4. 点击生成，数据会自动上传
```

### 技术文档（给 IT 部门）

- 项目 GitHub 仓库
- 部署平台信息（Streamlit Cloud）
- 管理员账号
- 环境变量配置（如果有）
- 数据库配置（如果有）

## 🔧 维护

### 更新代码

1. 修改代码
2. 提交到 GitHub
3. Streamlit Cloud 自动重新部署

### 监控

- Streamlit Cloud 提供基础监控
- 查看访问日志和错误

## 💰 成本

- **Streamlit Cloud**: 完全免费
- **Railway**: $5/月免费额度（通常够用）
- **Google Sheets**: 免费（15GB 空间）

## 🎯 最终推荐

**最佳方案**：
1. ✅ 使用 **Streamlit Cloud** 部署（免费，稳定）
2. ✅ 保留 Excel 下载功能（主要功能）
3. ✅ 可选添加 Google Sheets 集成（如果同事需要）
4. ✅ 不需要数据库（如果 RSS 覆盖够用）

**交接后**：
- 同事只需要访问网站
- 无需任何代码操作
- 你离职后完全不用管

## 🚨 注意事项

1. **不要提交敏感信息到 GitHub**
   - `google_credentials.json` 使用 `.gitignore`
   - 使用 Streamlit Secrets 存储

2. **定期检查**
   - 确保 Streamlit Cloud 正常运行
   - 检查是否有错误

3. **备份**
   - GitHub 仓库就是备份
   - 定期导出数据到 Google Sheets

